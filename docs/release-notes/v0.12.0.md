# Không Dịch Mobile v0.12.0

## Tính năng mới

- **Ảnh danh hiệu chưa hé lộ hiện icon khóa 🔒**: danh sách Danh và màn truyện
  theo danh hiện khóa placeholder thay vì icon trophy / ẩn hẳn — đồng bộ với
  web. Mỗi ngày hệ thống tự mở thêm 1 ảnh danh hiệu mới (theo chế độ hé lộ do
  admin cấu hình); **tên danh hiệu luôn hiển thị** để duyệt và tạo sự tò mò.

## Kỹ thuật

- 264 tests xanh, `flutter analyze` 0 lỗi.
- Model `DanhSummary` / `DanhInfo` đọc cờ `revealed` từ API — app tự phân biệt
  ảnh "chưa hé lộ" (khóa) và danh không có ảnh (trophy).

<!-- whatsnew:start -->
- Ảnh danh hiệu chưa hé lộ hiện icon khóa 🔒 (danh sách + màn truyện theo danh) — mỗi ngày tự mở thêm 1 ảnh mới như web; tên danh hiệu luôn hiển thị.
<!-- whatsnew:end -->